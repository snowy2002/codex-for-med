# 医学药学专业领域 Agent 架构讨论文档

## 1. 目标 Agent 分层架构

建议将专业领域 agent 按以下层级组织：

```text
用户任务层
  -> Skill 层
    -> Subagent 层
      -> Tool 层
      -> 模型层
      -> 知识库层
        -> 结构化数据库 / 文献全文库 / 向量索引 / 缓存
  -> 报告与结果输出层
```

每一层职责需要清晰，避免所有能力都堆在单一 tool 或 prompt 中。整体设计目标是让 agent 能够围绕医学药学场景完成外部数据库检索、文献知识查询、数据整理、专业报告生成、序列处理和垂直领域模型调用。

## 2. Tool 层

Tool 层负责低层、确定性的能力调用，应尽量保持单一职责、可测试、可复用。

当前和计划中的工具类型包括：

- 外部生物数据库 API：
  - PDB：结构检索、PDB 条目获取、结构元数据查询。
  - GenBank：核酸/蛋白序列记录检索。
  - UniProt：蛋白条目检索、关键词搜索、靶点信息查询。
- 本地 SQL 查询：
  - `antibody_training_records`：抗体、抗原、亲和力、序列、标签等训练数据查询。
  - `literature_documents`：论文/专利 md 原文和元数据查询。
  - `literature_documents_fts`：基于 FTS5 的全文检索。
- 后续可加入的专业工具：
  - 序列比对工具，例如 BLAST/MMseqs2。
  - 抗体编号和 CDR 标注工具，例如 ANARCI。
  - 结构预测或结构解析工具，例如 AlphaFold 相关接口或本地模型。
  - 文献导入、缓存刷新、索引重建工具。
  - 模型推理服务调用工具，例如亲和力预测、序列分类、文献抽取模型等。

Tool 层设计原则：

- 输入输出结构化。
- 工具内部处理 API 重试、超时、缓存和错误信息。
- 不让模型直接拼复杂业务流程。
- 对高风险操作保持只读或显式权限控制。
- 与模型层保持边界：tool 负责调用和封装，模型层负责模型部署、版本、输入输出协议和推理结果管理。

## 3. 知识库层

知识库层用于沉淀项目自有数据和外部资料的结构化结果。

当前知识库可以分为三类：

- 结构化训练数据：
  - 表：`antibody_training_records`
  - 内容：抗体序列、抗原 UniProt ID、靶点名称、亲和力值、标签、来源文献等。
- 文献原文知识库：
  - 表：`literature_documents`
  - 内容：论文/专利 markdown 原文、标题、文档类型、来源路径、hash、导入时间等。
- 全文检索索引：
  - 表：`literature_documents_fts`
  - 功能：支持对文献原文进行关键词检索和命中片段返回。

后续建议演进为：

```text
SQLite 主库
  - structured_records
  - literature_documents
  - literature_chunks
  - extracted_sequences
  - extracted_entities
  - model_predictions
  - task_outputs

FTS5 检索索引
  - 文献全文检索
  - chunk 检索

向量索引
  - 语义 RAG
  - 相似文献检索
  - 相似抗体/抗原描述检索
```

短期建议优先做 SQLite + FTS5，长期再加入 embedding 向量检索和 rerank。

## 4. Subagent 层

Subagent 层适合承载复杂、多步骤、可独立运行的专业流程。

建议接入的子 agent 类型包括：

- 文献抽取 agent：
  - 从论文/专利 md 中抽取靶点、抗体名称、SEQ ID、VH/VL/CDR、实验结果、亲和力数据。
- 数据清洗 agent：
  - 处理重复记录、单位归一化、靶点名称标准化、UniProt 映射。
- 数据入库 agent：
  - 将抽取结果写入结构化数据库，并维护 provenance。
- 评估 benchmark agent：
  - 固定参与 ABDataBench 或自有 benchmark 流程，负责样本构建、预测调用、打分和报告。
- 报告生成 agent：
  - 基于检索结果、结构化数据和模型输出生成分析报告。
- 预测 agent：
  - 调用模型层中的垂直领域任务模型，对抗原抗体序列组合或其他专业任务进行推理，并返回置信度和解释信息。

Subagent 设计原则：

- 每个子 agent 有清晰输入、输出和状态。
- 中间结果可落库，便于复现和审计。
- 支持批处理和单样本交互两种模式。
- 对失败任务支持重试、缓存和日志记录。
- 子 agent 不直接绑定某一个具体模型版本，而是通过模型层暴露的统一接口调用模型能力。

## 5. Skill 层

Skill 层面向用户任务，负责把工具、知识库、模型层和子 agent 编排成可复用工作流。

建议内置的领域 skill：

- 文献知识总结：
  - 输入专利号、论文路径或主题，输出结构化摘要、关键序列、靶点和实验结论。
- 抗体项目调研报告：
  - 输入靶点名称或 UniProt ID，检索数据库、文献和外部 API，生成研发背景报告。
- 抗原抗体序列整理：
  - 从文献中抽取 SEQ ID，整理 VH/VL/CDR、抗原序列和来源。
- 亲和力数据分析：
  - 查询 `antibody_training_records`，按靶点、抗体、实验类型分析亲和力分布。
- 预测任务执行：
  - 输入抗原和抗体序列，调用模型层中的亲和力预测模型，输出预测值、参考相似样本和解释。
- Benchmark 报告生成：
  - 汇总模型预测、真实标签、误差分布和失败案例。

Skill 层应允许用户自定义：

- 自定义报告模板。
- 自定义检索范围。
- 自定义输出格式，例如 md、csv、json、pdf。
- 自定义领域流程，例如只关注肿瘤免疫靶点、ADC 靶点或双抗设计。

## 6. 模型层

模型层用于部署、管理和调用我们自己训练的垂直领域任务模型。它不只是一组脚本，而应该作为专业 agent 的核心能力层，向 tool、subagent 和 skill 提供稳定的模型推理接口。

模型层主要职责：

- 管理模型版本、模型路径、配置和运行环境。
- 统一模型输入输出协议。
- 支持本地推理、HTTP 推理服务或批处理脚本三种部署形态。
- 记录模型预测结果、运行参数、输入 hash、模型版本和时间戳。
- 为 benchmark、报告生成和知识库更新提供可复现的模型输出。
- 支持后续继续接入新的垂直领域模型。

建议模型层抽象：

```text
模型层
  - affinity_prediction_model
  - sequence_annotation_model
  - literature_extraction_model
  - target_normalization_model
  - report_scoring_model
```

### 6.1 已实现子功能模型：抗原抗体亲和力预测模型

抗原抗体亲和力预测能力从原来的独立章节调整为模型层中的一个已实现子功能模型。它作为模型层的第一个核心模型能力，服务于预测任务、benchmark、训练数据分析和报告生成。

建议调用流程：

```text
输入抗原/抗体序列
  -> 序列合法性检查
  -> CDR/框架区标注
  -> 靶点和抗原信息补全
  -> 相似训练样本检索
  -> 亲和力预测模型调用
  -> 结果解释和可信度分析
  -> 预测结果入库
```

模型输入建议包括：

- 抗原序列或抗原 UniProt ID。
- 抗体 VH 序列。
- 抗体 VL 序列。
- 可选 CDR 序列。
- 可选靶点名称、物种、实验类型和来源文献。

模型输出建议包括：

- 预测亲和力值。
- 预测单位，例如 KD、nM 或 `log10(KD M)`。
- 模型版本。
- 置信度或不确定性估计。
- 相似训练样本。
- 关键解释信息。
- 预测结果写入 `model_predictions` 或同类结果表。

需要在会议中进一步确认的问题：

- 当前亲和力预测模型部署方式：本地模型、HTTP 服务还是脚本。
- 输入是否必须包含 VH/VL，还是支持 CDR、FASTA、PDB chain。
- 输出单位以 KD、nM、`log10(KD M)` 还是多任务指标为标准。
- 是否需要返回相似训练样本作为解释依据。
- 是否参与 ABDataBench 或自有 benchmark。
- 是否需要纳入单元测试、集成测试和 CI。

### 6.2 后续可接入模型

后续可以继续在模型层接入更多垂直领域模型：

- 文献序列抽取模型：
  - 从论文/专利中抽取 SEQ ID、VH/VL/CDR、抗原序列和实验结果。
- 靶点标准化模型：
  - 将原始靶点名称映射到标准靶点名称和 UniProt ID。
- 序列功能分类模型：
  - 对抗体序列、抗原片段或工程改造序列进行分类。
- 文献证据评分模型：
  - 对 RAG 检索结果进行相关性评分和证据质量排序。
- 报告质量评估模型：
  - 对生成报告进行完整性、一致性和引用质量检查。

